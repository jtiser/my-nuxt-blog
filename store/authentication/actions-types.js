const authenticateUser = 'authenticateUser'
const setLogoutTimer = 'setLogoutTimer'
const initAuth = 'initAuth'
const logout = 'logout'

export default {
  authenticateUser,
  setLogoutTimer,
  initAuth,
  logout
}
